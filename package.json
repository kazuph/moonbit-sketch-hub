{
  "name": "moonbit-sketch-hub",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node esbuild.js",
    "serve": "node esbuild.js && python3 -m http.server 9094 -d ./dist",
    "dev": "node esbuild.js && python3 -m http.server 9094 -d ./dist"
  },
  "dependencies": {
    "@moonbit/moonpad-monaco": "^0.1.202602254",
    "esbuild": "^0.27.4",
    "monaco-editor-core": "^0.52.2"
  }
}
